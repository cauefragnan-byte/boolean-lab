# A3 Álgebra Booleana — Sistema de Login

Projeto universitário que demonstra lógica booleana por meio de um sistema de login funcional. Após a autenticação, o usuário encontra uma explicação visual do operador AND, a tabela verdade e uma captura do código JavaScript real utilizado.

## Acesso para demonstração

- E-mail: `aluno@booleanlab.com`
- Senha: `boolean123`

O botão **Preencher** insere esses dados automaticamente.

Também existe um cenário incorreto pronto para demonstrar como `true && false` produz `false` e impede o acesso.

## Como executar

1. Abra a pasta `boolean-lab` no VS Code.
2. Abra o arquivo `index.html` no navegador.
3. Opcionalmente, use a extensão Live Server.

Não é necessário instalar dependências ou configurar um servidor.

## Tecnologias

- HTML5
- CSS3
- JavaScript puro

## Conceito demonstrado

O login compara o e-mail e a senha digitados. Cada comparação retorna um valor booleano. Os dois resultados são combinados com o operador AND (`&&`), que só libera o acesso quando ambos são verdadeiros.

## Integrantes

Cauê, Bernardo, Erick, Marcos, Bruno e Evandro.

Universidade São Judas Tadeu — Unidade Santana.
